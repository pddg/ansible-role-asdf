---
- hosts: localhost
  connection: local
  roles:
    - role: ../../
      vars:
        asdf_version: v0.8.1
        asdf_plugins:
          - name: golang
            versions:
              - 1.15.13
          - name: argocd
            repo: https://github.com/beardix/asdf-argocd.git
            versions:
              - 2.0.3
